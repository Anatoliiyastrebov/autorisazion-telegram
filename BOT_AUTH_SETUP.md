# 🤖 Настройка авторизации через Telegram бота

## 📋 Описание процесса

1. Пользователь выбирает анкету на сайте
2. Нажимает кнопку "Авторизоваться через Telegram"
3. Открывается Telegram Web App с формой подтверждения
4. Пользователь подтверждает авторизацию
5. Данные сохраняются на сервере с временным токеном
6. Пользователь возвращается на страницу анкеты
7. Данные автоматически подставляются в форму

---

## 🚀 Настройка Web App в BotFather

### Шаг 1: Откройте @BotFather в Telegram

### Шаг 2: Настройте Web App

1. Отправьте команду `/mybots`
2. Выберите вашего бота (`@telega_automat_bot`)
3. Нажмите **Bot Settings**
4. Нажмите **Menu Button** → **Configure Menu Button**
5. Введите:
   - **URL:** `https://autorisazion-telegram.vercel.app/auth/confirm`
   - **Button text:** `Авторизоваться`

### Шаг 3: Создайте Web App (для startapp параметра)

1. Отправьте команду `/newapp`
2. Выберите бота
3. Введите:
   - **Title:** `Авторизация`
   - **Description:** `Авторизация на сайте анкет`
   - **Web App URL:** `https://autorisazion-telegram.vercel.app/auth/confirm`
   - **Short Name:** `app` (ВАЖНО: именно "app")

После этого Web App будет доступен по ссылке:
```
https://t.me/telega_automat_bot/app?startapp=DATA
```

---

## 🔧 Настройка бота

### Файл `.env` в папке `bot/`

```env
TELEGRAM_BOT_TOKEN=8517653898:AAHwVsdsjErVCNoVGrHhVZUGptPWdrtOC2A
SITE_URL=https://autorisazion-telegram.vercel.app
```

### Установка и запуск

```bash
cd bot
npm install
node index.js
```

---

## 🌐 Переменные окружения на Vercel

Добавьте в **Settings** → **Environment Variables**:

| Переменная | Значение |
|------------|----------|
| `NEXT_PUBLIC_TELEGRAM_BOT_NAME` | `telega_automat_bot` |
| `TELEGRAM_BOT_TOKEN` | `8517653898:AAH...` |
| `TELEGRAM_GROUP_CHAT_ID` | `-5074397630` |
| `NEXT_PUBLIC_SITE_URL` | `https://autorisazion-telegram.vercel.app` |

---

## 🔄 Как работает авторизация

### 1. Пользователь на сайте нажимает "Авторизоваться через Telegram"

```
https://t.me/telega_automat_bot/app?startapp=BASE64_DATA
```

`BASE64_DATA` содержит URL возврата и тип анкеты.

### 2. Telegram открывает Web App `/auth/confirm`

Web App получает:
- `window.Telegram.WebApp.initDataUnsafe.user` - данные пользователя
- `window.Telegram.WebApp.initDataUnsafe.start_param` - закодированные данные (URL возврата)

### 3. Пользователь подтверждает авторизацию

- Данные отправляются на `/api/auth/save-user`
- Сервер сохраняет данные и возвращает `auth_token`
- Web App показывает кнопку "Вернуться к анкете"

### 4. Возврат на сайт

- Пользователь нажимает кнопку
- Открывается URL: `/questionnaire/baby?auth_token=TOKEN`
- Страница загружает данные через `/api/auth/get-user-data?token=TOKEN`
- Форма автоматически заполняется

---

## ✅ Проверка работы

1. Откройте сайт: https://autorisazion-telegram.vercel.app
2. Выберите анкету (например, "Детская анкета")
3. Нажмите "Авторизоваться через Telegram"
4. Должен открыться Telegram Web App
5. Подтвердите авторизацию
6. Нажмите "Вернуться к анкете"
7. Данные из Telegram должны автоматически заполниться

---

## 🔧 Устранение проблем

### Проблема: Web App не открывается

**Проверьте:**
- Создан ли Web App в BotFather с short_name `app`
- Правильный ли URL: `https://t.me/telega_automat_bot/app?startapp=...`

### Проблема: Данные не загружаются после возврата

**Проверьте:**
- Токен действителен 10 минут
- API `/api/auth/get-user-data` возвращает данные
- В URL есть параметр `auth_token`

### Проблема: start_param пустой

**Проверьте:**
- Web App открыт через ссылку с `?startapp=` параметром
- Данные правильно закодированы в Base64

---

## 📚 Дополнительные ресурсы

- [Telegram Web Apps Documentation](https://core.telegram.org/bots/webapps)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

**Готово!** Авторизация через Telegram Web App настроена. 🎉
