# 🤖 Настройка авторизации через Telegram бота

## 📋 Описание процесса

1. Пользователь нажимает кнопку "Войти через Telegram" на сайте
2. Открывается бот в Telegram
3. Бот отправляет сообщение с кнопкой "Подтвердить авторизацию"
4. При нажатии на кнопку открывается диалог подтверждения
5. После подтверждения пользователь возвращается на сайт и автоматически авторизуется

---

## 🚀 Настройка бота

### Шаг 1: Создайте бэкенд для бота

Вам нужно создать простой сервер для обработки команд бота. Пример на Node.js:

```javascript
const TelegramBot = require('node-telegram-bot-api')
const axios = require('axios')

const bot = new TelegramBot(process.env.TELEGRAM_BOT_TOKEN, { polling: true })
const SITE_URL = process.env.SITE_URL || 'https://ваш-домен.vercel.app'

// Обработчик команды /start
bot.onText(/\/start auth_(.+)/, async (msg, match) => {
  const chatId = msg.chat.id
  const sessionId = match[1]
  
  // Получаем токен авторизации с сайта
  try {
    const response = await axios.post(`${SITE_URL}/api/auth/generate-token`, {
      userId: chatId,
      botToken: process.env.TELEGRAM_BOT_TOKEN
    })
    
    const { token, callbackUrl } = response.data
    
    // Сохраняем токен в сессии (можно использовать Redis или БД)
    // Для простоты используем временное хранилище
    bot.sessionTokens = bot.sessionTokens || {}
    bot.sessionTokens[sessionId] = { token, userId: chatId }
    
    // Отправляем сообщение с кнопкой подтверждения
    bot.sendMessage(chatId, 'Для авторизации на сайте нажмите кнопку ниже:', {
      reply_markup: {
        inline_keyboard: [[
          {
            text: '✅ Подтвердить авторизацию',
            callback_data: `confirm_auth_${sessionId}`
          }
        ]]
      }
    })
  } catch (error) {
    console.error('Ошибка при генерации токена:', error)
    bot.sendMessage(chatId, '❌ Ошибка при создании токена авторизации. Попробуйте еще раз.')
  }
})

// Обработчик callback кнопки
bot.on('callback_query', async (query) => {
  const chatId = query.message.chat.id
  const data = query.data
  
  if (data.startsWith('confirm_auth_')) {
    const sessionId = data.replace('confirm_auth_', '')
    const sessionData = bot.sessionTokens?.[sessionId]
    
    if (!sessionData) {
      bot.answerCallbackQuery(query.id, {
        text: 'Сессия истекла. Начните авторизацию заново.',
        show_alert: true
      })
      return
    }
    
    // Показываем диалог подтверждения
    bot.answerCallbackQuery(query.id, {
      text: 'Вы уверены, что хотите авторизоваться на сайте?',
      show_alert: true
    })
    
    // Отправляем кнопку с Web App или прямой ссылкой
    bot.sendMessage(chatId, 'Нажмите кнопку ниже, чтобы завершить авторизацию:', {
      reply_markup: {
        inline_keyboard: [[
          {
            text: '🔐 Завершить авторизацию',
            url: `${SITE_URL}/api/auth/callback?token=${sessionData.token}&user_id=${sessionData.userId}`
          }
        ]]
      }
    })
    
    // Удаляем токен из сессии
    delete bot.sessionTokens[sessionId]
  }
})

// Обработчик обычной команды /start
bot.onText(/\/start/, (msg) => {
  const chatId = msg.chat.id
  bot.sendMessage(chatId, 'Привет! Для авторизации на сайте перейдите на сайт и нажмите кнопку "Войти через Telegram".')
})
```

### Шаг 2: Установите зависимости

```bash
npm install node-telegram-bot-api axios
```

### Шаг 3: Настройте переменные окружения

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
SITE_URL=https://ваш-домен.vercel.app
```

### Шаг 4: Запустите бота

```bash
node bot.js
```

---

## 🔄 Альтернативный вариант (без отдельного сервера)

Если у вас нет возможности запустить отдельный сервер для бота, можно использовать сервисы типа:
- [Glitch](https://glitch.com/)
- [Heroku](https://www.heroku.com/)
- [Railway](https://railway.app/)
- [Render](https://render.com/)

Или использовать вебхуки вместо polling (требует HTTPS).

---

## 📝 Настройка на Vercel

Добавьте переменную окружения `NEXT_PUBLIC_SITE_URL` в Vercel:

1. Перейдите в **Settings** → **Environment Variables**
2. Добавьте:
   - `NEXT_PUBLIC_SITE_URL` = `https://ваш-домен.vercel.app`

---

## ✅ Проверка работы

1. Откройте сайт
2. Нажмите "Войти через Telegram"
3. Должен открыться бот в Telegram
4. Бот должен отправить сообщение с кнопкой "Подтвердить авторизацию"
5. Нажмите на кнопку
6. Должен появиться диалог подтверждения
7. Нажмите "Завершить авторизацию"
8. Должен произойти возврат на сайт с автоматической авторизацией

---

## 🔧 Устранение проблем

### Проблема: Бот не отвечает на команду /start

**Решение:**
- Проверьте, что бот запущен
- Проверьте токен бота
- Проверьте логи бота

### Проблема: Токен не генерируется

**Решение:**
- Проверьте, что `TELEGRAM_BOT_TOKEN` правильный
- Проверьте, что API endpoint `/api/auth/generate-token` доступен
- Проверьте логи на Vercel

### Проблема: Авторизация не происходит после возврата на сайт

**Решение:**
- Проверьте, что URL callback правильный
- Проверьте консоль браузера на наличие ошибок
- Проверьте, что токен не истек (действителен 5 минут)

---

## 📚 Дополнительные ресурсы

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [node-telegram-bot-api](https://github.com/yagop/node-telegram-bot-api)

---

**Готово!** Теперь авторизация работает через бота с подтверждением. 🎉

