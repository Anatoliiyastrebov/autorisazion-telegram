# Health Questionnaire App (Анкета по здоровью)

Веб-приложение для сбора анкет с авторизацией через Telegram.

## 🚀 Возможности

- ✅ Авторизация через Telegram (Telegram Login Widget и Telegram Web App)
- ✅ Четыре типа анкет: для женщин, мужчин, базовая и расширенная
- ✅ Отправка анкет в Telegram группу
- ✅ Адаптивный дизайн

## 📋 Требования

- Node.js 18+ и npm
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- Telegram Bot Name (получить у [@BotFather](https://t.me/BotFather))
- Vercel аккаунт (для деплоя)

## 🛠️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://github.com/Anatoliiyastrebov/autorisazion-telegram.git
cd autorisazion-telegram
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_TELEGRAM_BOT_NAME=telega_automat_bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_GROUP_CHAT_ID=-5074397630
TELEGRAM_ADMIN_CHAT_ID=your_chat_id_here
```

**Важно:**
- Не коммитьте файл `.env.local` в репозиторий!
- Для продакшена на Vercel настройте переменные окружения в Dashboard

### 4. Запуск dev-сервера

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## 📦 Сборка для продакшена

```bash
npm run build
npm start
```

## 🌐 Деплой на Vercel

### Деплой через GitHub:

1. **Подключите репозиторий к Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Нажмите **"Add New Project"**
   - Импортируйте ваш GitHub репозиторий
   - Vercel автоматически определит настройки из `vercel.json`

2. **Настройка переменных окружения:**
   - После первого деплоя, перейдите в **Project Settings** → **Environment Variables**
   - Добавьте переменные:
     - **NEXT_PUBLIC_TELEGRAM_BOT_NAME** - имя Telegram бота
     - **TELEGRAM_BOT_TOKEN** - токен Telegram бота
     - **TELEGRAM_GROUP_CHAT_ID** - ID группы для отправки анкет (опционально)
     - **TELEGRAM_ADMIN_CHAT_ID** - ID администратора для уведомлений (опционально)
   - Выберите окружения (Production, Preview, Development)
   - Нажмите **"Save"** для каждой переменной

3. **Пересборка:**
   - Перейдите в **Deployments**
   - Нажмите на три точки рядом с последним деплоем → **"Redeploy"**
   - Или просто сделайте новый коммит в GitHub (автоматический деплой)

## 🔧 Технологии

- **Next.js 14** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **Telegram Bot API** - интеграция с Telegram

## 📝 Структура проекта

```
app/
├── api/
│   └── submit/          # API route для отправки анкет
├── auth/
│   └── confirm/        # Страница подтверждения авторизации
├── questionnaire/      # Страницы анкет
└── page.tsx            # Главная страница

components/
├── QuestionnaireForm.tsx  # Форма анкеты
└── TelegramLogin.tsx      # Компонент авторизации через Telegram
```

## 📍 Где получить значения переменных

### Telegram Bot Token и Bot Name:
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` или `/mybots`
3. Скопируйте токен и имя бота

### Telegram Chat ID:
1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте любое сообщение
3. Бот вернет ваш `chat_id`

### Telegram Group Chat ID:
1. Добавьте бота [@userinfobot](https://t.me/userinfobot) в группу
2. Отправьте любое сообщение в группу
3. Бот вернет `chat_id` группы

## 🔐 Безопасность

- ✅ Все секретные ключи хранятся только в переменных окружения Vercel
- ✅ Файл `.env.local` в `.gitignore` (не попадает в репозиторий)
- ✅ API endpoints защищены проверкой подписи Telegram
- ✅ Ошибки не раскрывают детали реализации клиенту

**Важно:**
- Никогда не публикуйте `TELEGRAM_BOT_TOKEN` в клиентском коде!
- Используйте HTTPS (обязательно в продакшене)

## 📄 Лицензия

MIT

## 👤 Автор

Создано с помощью Lovable
