# Настройка Supabase для хранения анкет

## Шаг 1: Создание проекта в Supabase

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard/org/eghpuajkxdvrvcveokcx)
2. Нажмите **"New project"** (если проект еще не создан)
3. Заполните данные проекта и дождитесь создания (2-3 минуты)

## Шаг 2: Применение SQL миграции

1. В вашем проекте Supabase перейдите в **SQL Editor** (левое меню)
2. Нажмите **"New query"**
3. Откройте файл `supabase/migrations/001_initial_schema.sql` в этом проекте
4. Скопируйте **весь** SQL код из файла
5. Вставьте в SQL Editor в Supabase
6. Нажмите **"Run"** (или `Ctrl+Enter` / `Cmd+Enter`)
7. Должно появиться сообщение "Success. No rows returned"

⚠️ **Примечание**: Если вы получили ошибку о том, что триггер или таблица уже существует, это нормально. SQL миграция теперь автоматически удаляет существующие объекты перед созданием новых. Просто выполните SQL еще раз - он должен пройти успешно.

Это создаст необходимые таблицы:
- `otp_codes` - для хранения одноразовых кодов
- `sessions` - для хранения сессионных токенов
- `questionnaires` - для хранения зашифрованных анкет

## Шаг 3: Получение ключей доступа

### Где найти ключи в Supabase Dashboard:

1. **Откройте ваш проект в Supabase Dashboard**
   - Перейдите на [https://supabase.com/dashboard/org/eghpuajkxdvrvcveokcx](https://supabase.com/dashboard/org/eghpuajkxdvrvcveokcx)
   - Выберите ваш проект (или создайте новый, если еще не создан)

2. **Перейдите в Settings**
   - В левом боковом меню найдите иконку **⚙️ Settings** (в самом низу меню)
   - Нажмите на неё

3. **Откройте раздел API**
   - В меню Settings выберите **"API"** (обычно второй пункт после "General")
   - Или перейдите напрямую: после выбора проекта, в URL будет что-то вроде `https://supabase.com/dashboard/project/xxxxx/settings/api`

4. **Найдите секцию "Project API keys"**
   - На странице API вы увидите несколько секций
   - Найдите секцию **"Project API keys"** (обычно вверху страницы)

5. **Скопируйте Project URL**
   - В секции "Project URL" вы увидите URL вида: `https://xxxxx.supabase.co`
   - Нажмите на иконку копирования рядом с URL или выделите и скопируйте вручную

6. **Скопируйте service_role key**
   - В секции "Project API keys" вы увидите два ключа:
     - **anon** `public` key (этот НЕ нужен)
     - **service_role** `secret` key (этот нужен!)
   - Напротив `service_role` key нажмите кнопку **"Reveal"** (или "Show") чтобы увидеть ключ
   - После этого нажмите на иконку копирования или выделите и скопируйте ключ

⚠️ **ВАЖНО**: 
- `service_role` key имеет полный доступ к базе данных
- Никогда не публикуйте его в клиентском коде!
- Используйте только в переменных окружения на сервере (Vercel)

### Визуальное описание:
```
Supabase Dashboard
├── [Ваш проект] (выберите проект)
│   ├── Table Editor
│   ├── SQL Editor
│   ├── Authentication
│   ├── ...
│   └── ⚙️ Settings (внизу меню)
│       ├── General
│       ├── API ← ВОТ ТУТ!
│       │   ├── Project URL: https://xxxxx.supabase.co
│       │   └── Project API keys:
│       │       ├── anon public: eyJ... (НЕ нужен)
│       │       └── service_role secret: eyJ... ← ВОТ ЭТОТ!
│       ├── Database
│       └── ...
```

## Шаг 4: Генерация ключа шифрования

Выполните в терминале:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Скопируйте полученный ключ (64 символа hex).

## Шаг 5: Настройка переменных окружения в Vercel

1. Откройте ваш проект в [Vercel Dashboard](https://vercel.com/dashboard)
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте следующие переменные:

### Переменная 1: SUPABASE_URL
- **Key**: `SUPABASE_URL`
- **Value**: Ваш Project URL из шага 3
- **Environment**: Production, Preview, Development (выберите все)

### Переменная 2: SUPABASE_SERVICE_ROLE_KEY
- **Key**: `SUPABASE_SERVICE_ROLE_KEY`
- **Value**: Ваш service_role key из шага 3
- **Environment**: Production, Preview, Development (выберите все)

### Переменная 3: ENCRYPTION_KEY
- **Key**: `ENCRYPTION_KEY`
- **Value**: Сгенерированный ключ из шага 4
- **Environment**: Production, Preview, Development (выберите все)

4. Нажмите **"Save"** для каждой переменной

## Шаг 6: Установка зависимостей

Убедитесь, что установлен пакет Supabase:

```bash
npm install
```

Это установит `@supabase/supabase-js` (уже добавлен в package.json).

## Шаг 6.5: Настройка Telegram Webhook (для автоматической отправки OTP)

Для автоматической отправки OTP кодов пользователям, нужно настроить Telegram webhook:

1. **Примените дополнительную SQL миграцию:**
   - Откройте Supabase Dashboard → SQL Editor
   - Скопируйте содержимое файла `supabase/migrations/002_add_telegram_chat_ids.sql`
   - Вставьте и выполните (Run)

2. **Получите URL вашего webhook:**
   - После деплоя на Vercel, ваш webhook будет доступен по адресу:
   - `https://your-domain.vercel.app/api/telegram/webhook`

3. **Настройте webhook в Telegram:**
   ```bash
   curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook" \
     -H "Content-Type: application/json" \
     -d '{"url": "https://your-domain.vercel.app/api/telegram/webhook"}'
   ```

   Или используйте браузер:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://your-domain.vercel.app/api/telegram/webhook
   ```

4. **Проверьте webhook:**
   ```bash
   curl "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getWebhookInfo"
   ```

**Как это работает:**
- Когда пользователь пишет боту (например, `/start`), Telegram отправляет обновление на webhook
- Webhook сохраняет `chat_id` пользователя в базе данных
- При запросе OTP система использует сохраненный `chat_id` для отправки кода

**Важно:** Пользователь должен хотя бы раз написать боту (например, `/start`), чтобы его `chat_id` был сохранен.

## Шаг 7: Деплой на Vercel

После добавления переменных окружения:

1. Сделайте коммит и пуш в GitHub (если используете автоматический деплой)
2. Или перейдите в Vercel Dashboard → **Deployments**
3. Нажмите **"..."** на последнем деплое → **"Redeploy"**

⚠️ **ВАЖНО**: Vercel встраивает переменные окружения во время сборки. После добавления переменных обязательно пересоберите проект!

## Проверка работы

После деплоя:

1. Откройте ваше приложение
2. Попробуйте отправить тестовую анкету
3. Проверьте в Supabase Dashboard:
   - Перейдите в **Table Editor** → **questionnaires**
   - Должна появиться запись с зашифрованными данными

## Устранение проблем

### Ошибка "Supabase URL and Service Role Key must be set"
- Убедитесь, что переменные окружения добавлены в Vercel
- Убедитесь, что проект был пересобран после добавления переменных
- Проверьте, что переменные добавлены для правильного окружения

### Ошибка "relation does not exist"
- Убедитесь, что SQL миграция была выполнена успешно
- Проверьте в Supabase Dashboard → **Table Editor**, что таблицы существуют

### Ошибка "Invalid API key"
- Проверьте, что вы скопировали правильный `service_role` key (не `anon` key)
- Убедитесь, что ключ не содержит лишних пробелов

## Готово! 🎉

Теперь все анкеты будут безопасно храниться в Supabase с шифрованием данных.
